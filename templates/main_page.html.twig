{% extends 'base.html.twig' %}

{% block body %}
    {% if app.session and app.session.get('username') %}
        <a href="">Logout</a>
    {% else %}
        <a href="{{ path('login') }}">Login</a>
    {% endif %}
{% endblock %}
